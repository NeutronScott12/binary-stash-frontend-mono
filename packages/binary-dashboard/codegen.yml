schema: http://localhost:3636/graphql
documents: ./src/**/*.graphql
overwrite: true
generates:
    ./src/apollo/components/apollo-components.tsx:
        config:
            noNamespaces: true
        plugins:
            - typescript-common:
            - typescript-client:
            - typescript-react-apollo:
